# OpenAI (for embeddings and LLM)
OPENAI_API_KEY=sk-...

# Optional: use a local LLM via Ollama instead (set to "ollama" to use)
# LLM_PROVIDER=openai
# LLM_PROVIDER=ollama

# Optional overrides (defaults in src/config.py)
# CHUNK_SIZE=512
# CHUNK_OVERLAP=64
# RETRIEVAL_TOP_K=4
# GATE_CONFIDENCE_THRESHOLD=0.7
# GATE_MIN_CHUNKS=1
